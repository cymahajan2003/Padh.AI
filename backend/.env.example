Create a `.env` file in the `backend` directory with the following content:
Make sure to replace `your_groq_api_key_here` with your actual GROQ API key in the `.env` file.
GROQ_API_KEY=your_groq_api_key_here